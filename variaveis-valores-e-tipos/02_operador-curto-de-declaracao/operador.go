package main

import "fmt"

func main() {
	// Em Go usamos := operador curto de declaração para declararmos as nossas variáveis.
	// Ele tem tipagem automática, ao declarar ele já identifica o tipo
	// Sempre que usar, tem que declarar uma variável nova, ao tentar declarar a mesma variável, o programa dará erro
	// Só funciona dentro do codeblocks, ou seja, dentro do escopo da func.


	x := 10
	fmt.Println(x)
}